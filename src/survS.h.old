/*
** Version of the file for S3.4, Sun
*** works with R on Solaris 2.7 (TSL)
*/

#define _TIME_H   /* a hack to stop inclusion of time.h */
#define _TIME_H_ 
#include "S.h"
#define ALLOC(a,b) S_alloc(a,b)
#include "Rinternals.h"
