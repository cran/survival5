Terms
source("../../R/model.newframe.s")
sfit <- survfit(fitx,c(fitx$means[1], 0))
sfit
fitx$Terms
terms(fitx)
fitx$terms
c(fitx$means[1], 0)
source("../../R/model.newframe.s")
sfit <- survfit(fitx,c(fitx$means[1], 0))
debug(model.newframe)
sfit <- survfit(fitx,c(fitx$means[1], 0))
newdata
fitx$coef
nvar
object
source("../../R/model.newframe.s")
data(jasa1)
data(heart)
attach(jasa1)
data(ratetables)
attr(survexp.usr,"dimid")
names(attributes(survexp.usr))
attr(survexp.usr,"cutpoints")
dim(survexp.usr)
q()
fit3$means
fit1
debug(model.newframe)
nvar
ls()
source("../../R/model.newframe.s")
